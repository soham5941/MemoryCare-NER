{
  "name": "memorycare-ner-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Express API gateway for the MemoryCare NER demo",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check routes/nerRoutes.js && node --check controllers/nerController.js && node --check services/pythonService.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.21.2"
  }
}
